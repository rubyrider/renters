<%= form_with(model: dashboard_sms_account, local: true) do |form| %>
  <% if dashboard_sms_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard_sms_account.errors.count, "error") %> prohibited this dashboard_sms_account from being saved:</h2>

      <ul>
      <% dashboard_sms_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
