<%= form_with(model: dashboard_collection, local: true) do |form| %>
  <% if dashboard_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard_collection.errors.count, "error") %> prohibited this dashboard_collection from being saved:</h2>

      <ul>
      <% dashboard_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
