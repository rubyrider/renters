<p id="notice"><%= notice %></p>

<h1>Dashboard Sms Accounts</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dashboard_sms_accounts.each do |dashboard_sms_account| %>
      <tr>
        <td><%= link_to 'Show', dashboard_sms_account %></td>
        <td><%= link_to 'Edit', edit_dashboard_sms_account_path(dashboard_sms_account) %></td>
        <td><%= link_to 'Destroy', dashboard_sms_account, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dashboard Sms Account', new_dashboard_sms_account_path %>
